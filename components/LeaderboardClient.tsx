'use client';
import type {CSSProperties} from 'react';import {useState} from 'react';import Image from 'next/image';import type {LeaderboardRow} from '@/lib/types';import {MobileHeader} from '@/components/MobileHeader';import {MobileNav} from '@/components/MobileNav';
function Avatar({url}:{url:string|null}){return <span className="relative grid h-9 w-9 place-items-center overflow-hidden rounded-full bg-[#e3f2fb]">{url?<Image src={url} alt="" fill sizes="36px" className="object-cover"/>:<span className="fish text-sm"/>}</span>}
export function LeaderboardClient({style,current,previous,userId}:{style?:CSSProperties;current:LeaderboardRow[];previous:LeaderboardRow[];userId:string}){const [period,setPeriod]=useState<'current'|'previous'>('current'),rows=period==='current'?current:previous,mine=rows.find(row=>row.userId===userId),visible=rows.slice(0,50);return <main className="phone-shell" style={style}><MobileHeader title="养渝英雄榜" back="/me"/><div className="page-pad"><div className="mb-4 flex justify-center gap-2">{([['current','本周榜'],['previous','上周榜']] as const).map(([value,label])=><button onClick={()=>setPeriod(value)} className={`pill ${period===value?'active':''}`} key={value}>{label}</button>)}</div><section className="card overflow-hidden">{visible.map(row=><div className={`flex items-center gap-3 border-b border-[#edf3f7] px-4 py-3 text-sm last:border-0 ${row.userId===userId?'bg-[#eef7fc]':''}`} key={row.userId}><b className="w-9">#{row.rank}</b><Avatar url={row.avatarUrl}/><span className="min-w-0 flex-1 truncate font-bold">{row.nickname}</span><span>{row.actionCount} 次</span></div>)}{!rows.length&&<div className="p-5 text-sm text-[#7890a6]">{period==='current'?'本周暂无榜单数据':'上周暂无榜单数据'}</div>}</section>{mine&&!visible.some(row=>row.userId===userId)&&<div className="sticky bottom-20 mt-4 flex items-center gap-3 rounded-2xl bg-[#183b61] px-4 py-4 text-sm text-white shadow-lg"><b>#{mine.rank}</b><Avatar url={mine.avatarUrl}/><span className="min-w-0 flex-1 truncate">{mine.nickname}</span><span>{mine.actionCount} 次</span></div>}{!mine&&rows.length>0&&<div className="mt-4 rounded-2xl bg-[#eef7fc] px-4 py-3 text-sm text-[#7890a6]">我的排名　暂未上榜</div>}</div><MobileNav/></main>}
