'use client';import {useState} from 'react';export function ProgressControl({initial,goal}:{initial:number;goal:number}){const [n,setN]=useState(initial);return <div className="flex items-center gap-3"><button onClick={()=>setN(Math.max(0,n-1))} className="h-9 w-9 rounded-full border border-[#dbeaf4] bg-white text-lg">−</button><b className="min-w-14 text-center">{n} / {goal}</b><button onClick={()=>setN(Math.min(goal,n+1))} className="h-9 w-9 rounded-full border-0 bg-[#eaf5fc] text-[#3785cc] text-lg">＋</button></div>}
