'use client';import {useState} from 'react';import type {Task} from '@/lib/types';import {TaskCard} from './TaskCard';export function UrgentList({tasks}:{tasks:Task[]}){const [cat,setCat]=useState('全部');const cats=['全部','音乐','数据','商务'];return <><div className="flex gap-2 overflow-x-auto pb-3">{cats.map(c=><button key={c} onClick={()=>setCat(c)} className={`pill ${cat===c?'active':''}`}>{c}</button>)}</div><div className="space-y-3">{tasks.filter(t=>cat==='全部'||t.category===cat).slice(0,3).map(t=><TaskCard key={t.id} task={t}/>)}</div></>}
