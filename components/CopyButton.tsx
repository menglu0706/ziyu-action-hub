'use client';import {useState} from 'react';export function CopyButton({text}:{text:string}){const [ok,setOk]=useState(false);async function copy(){await navigator.clipboard.writeText(text);setOk(true);setTimeout(()=>setOk(false),1500)}return <button type="button" className="pill active" onClick={copy}>{ok?'已复制 ✓':'复制'}</button>}
