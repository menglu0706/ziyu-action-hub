'use client';import {useState} from 'react';export function CopyButton({text}:{text:string}){const [ok,setOk]=useState(false);async function copy(){await navigator.clipboard.writeText(text);setOk(true);setTimeout(()=>setOk(false),1500)}return <><button className="pill active" onClick={copy}>复制</button>{ok&&<div className="fixed bottom-24 left-1/2 z-50 -translate-x-1/2 rounded-full bg-[#173658] px-5 py-2 text-sm text-white shadow-lg">已复制</div>}</>}
