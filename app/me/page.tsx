/* eslint-disable @next/next/no-img-element */
import Link from 'next/link';
import {MobileNav} from '@/components/MobileNav';
import {TimezoneSync} from '@/components/TimezoneSync';
import {repository} from '@/lib/repository';
import {getUserDashboard} from '@/lib/user-repository';
import {visualStyle} from '@/lib/visual';
import './me.css';
export const dynamic='force-dynamic';
const shortcuts=[['◷','我的任务记录','/me/tasks','累计行动记录'],['♙','我的账号','/me/account','平台账号数量'],['☆','我的收藏','#','收藏内容'],['▤','攻略 & Tips','/guide','攻略内容与小Tips'],['⚙','设置','/me/account','账号与偏好设置']];
function Avatar({url,hero=false}:{url:string|null;hero?:boolean}){return <div className={`me-avatar grid shrink-0 place-items-center overflow-hidden rounded-full bg-[#dff1fb] ${hero?'is-hero':''}`}>{url?<img src={url} alt="" className="h-full w-full object-cover" loading="lazy"/>:<span className="fish"/>}</div>}
export default async function Me(){
  const [setting,dashboard]=await Promise.all([repository.getVisualSetting('me'),getUserDashboard()]);const {user,profile,stats,leaderboard}=dashboard;const mine=leaderboard.find(row=>row.userId===user.id),top=leaderboard.slice(0,5).filter(row=>row.userId!==user.id),streak=stats.streak===null?'—':`${stats.streak}天`;
  return <main className="phone-shell me-page"><TimezoneSync current={profile.timezone}/><div className="page-pad me-page-content"><section className="card me-profile-card overflow-hidden"><div className="me-profile-visual" style={visualStyle(setting)}><div className="me-profile-content flex items-center gap-3"><Avatar url={profile.avatar_url} hero/><div className="min-w-0 flex-1"><b className="me-profile-name block truncate">{profile.nickname}</b><span className="me-brand block">All for ZIYU</span></div><Link href="/me/account" className="me-edit-profile no-underline">编辑资料</Link></div></div><div className="me-stats grid grid-cols-4 text-center">{[['今日行动',stats.today??'—'],['本周行动',stats.week??'—'],['连续养渝',streak],['累计行动',stats.total]].map(([label,value])=><div className="me-stat" key={label}><b className="block">{value}</b><span>{label}</span></div>)}</div></section><section className="card me-leaderboard-card overflow-hidden"><div className="me-leaderboard-head flex items-center justify-between border-b border-[#edf2f6]"><h2 className="me-section-title m-0">养渝英雄榜 <span>· 本周</span></h2><Link href="/me/leaderboard" className="me-leaderboard-link no-underline">查看完整榜单 →</Link></div><div className="me-leaderboard-list divide-y divide-[#edf2f6]">{mine?<div className="me-rank-row flex items-center gap-3"><b className="me-own-rank">我的排名 #{mine.rank}</b><Avatar url={mine.avatarUrl}/><span className="min-w-0 flex-1 truncate font-semibold">{mine.nickname}</span><b className="me-action-count">{mine.actionCount}次</b></div>:<div className="me-empty-rank">我的排名　暂未上榜</div>}{top.map(row=><div className="me-leader-row flex items-center gap-3" key={row.userId}><b className={`me-rank-number rank-${row.rank}`}>{row.rank}</b><Avatar url={row.avatarUrl}/><span className="min-w-0 flex-1 truncate font-semibold">{row.nickname}</span><span className="me-action-count">{row.actionCount}次</span></div>)}{!leaderboard.length&&<div className="me-empty-list">本周暂无榜单数据</div>}</div></section><div className="me-shortcut-grid grid grid-cols-2">{shortcuts.map(([icon,label,href,subtitle],index)=><Link key={label} href={href} className={`card me-shortcut flex items-center text-[#294663] no-underline ${index===4?'col-span-2':''}`}><span className="me-shortcut-icon" aria-hidden="true">{icon}</span><span className="min-w-0 flex-1"><b className="block">{label}</b><span className="block truncate">{subtitle}</span></span><span className="me-shortcut-chevron" aria-hidden="true">›</span></Link>)}</div></div><MobileNav/></main>;
}
