import {LeaderboardClient} from '@/components/LeaderboardClient';import {repository} from '@/lib/repository';import {getLeaderboards} from '@/lib/user-repository';import {visualStyle} from '@/lib/visual';export const dynamic='force-dynamic';export default async function Leaderboard(){const [setting,{user,current,previous}]=await Promise.all([repository.getVisualSetting('leaderboard'),getLeaderboards()]);return <LeaderboardClient style={visualStyle(setting)} current={current} previous={previous} userId={user.id}/>}
