import {MobileHeader} from '@/components/MobileHeader';import {AccountForm} from '@/components/AccountForm';import {requireUser} from '@/lib/auth/user';export const dynamic='force-dynamic';export default async function Account(){const {user,profile}=await requireUser('/me/account');return <main className="phone-shell"><MobileHeader title="我的账号管理" back="/me"/><div className="page-pad"><section className="card p-5"><AccountForm email={user.email??''} nickname={profile.nickname}/></section></div></main>}
