import {MobileHeader} from '@/components/MobileHeader';import {AccountForm} from '@/components/AccountForm';import {getOrCreateOwnProfile,requireUserIdentity} from '@/lib/auth/user';export const dynamic='force-dynamic';export default async function Account(){const context=await requireUserIdentity('/me/account');const {db,user}=context;const [profile,accountResult]=await Promise.all([getOrCreateOwnProfile(context),db.from('platform_account_counts').select('platform,account_count').eq('user_id',user.id).order('platform')]);return <main className="phone-shell"><MobileHeader title="我的账号管理" back="/me"/><div className="page-pad"><section className="card p-5"><AccountForm email={user.email??''} nickname={profile.nickname} leaderboardOptIn={profile.leaderboard_opt_in} accounts={accountResult.data??[]}/></section></div></main>}
