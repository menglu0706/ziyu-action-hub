import './admin.css';import {AdminPrototype} from '@/components/AdminPrototype';import {TaskAdminList,TaskAdminNew} from '@/components/TaskAdminFlow';import {requireAdmin} from '@/lib/auth/admin';import {getAdminInitialData} from '@/lib/admin-repository';export const dynamic='force-dynamic';export default async function Admin({params,searchParams}:{params:Promise<{slug?:string[]}>;searchParams:Promise<Record<string,string|string[]|undefined>>}){await requireAdmin();const [{slug=[]},query,data]=await Promise.all([params,searchParams,getAdminInitialData()]);const path=slug.join('/');const value=(key:string)=>typeof query[key]==='string'?query[key] as string:undefined;if(path==='tasks/new'){const preset=value('preset'),editId=value('edit');return <TaskAdminNew key={`${preset??''}:${editId??''}`} tasks={data.tasks} preset={preset} editId={editId}/>}if(path==='tasks')return <TaskAdminList tasks={data.tasks}/>;return <AdminPrototype slug={slug} initialData={data} query={query}/>}
