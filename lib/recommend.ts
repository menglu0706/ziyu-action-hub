import type {Task} from './types';const deadlineTime=(value:string|null)=>value?new Date(value).getTime():Number.POSITIVE_INFINITY;export function recommend(tasks:Task[],minutes:number){return [...tasks].filter(t=>t.daily&&t.minutes<=minutes).sort((a,b)=>Number(b.pinned)-Number(a.pinned)||deadlineTime(a.deadline)-deadlineTime(b.deadline)||b.urgency-a.urgency||b.required-a.required||a.minutes-b.minutes)}
